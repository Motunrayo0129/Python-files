def multiply_number(number1, number2):
	multiply = 0
	for number in range(number2):
		multiply += number1
	return multiply

	code = 0
	for number in range(-nummber2):
		code += -num1
	return code

num1 = int(input('Enter an integer1: '))
num2 = int(input('Enter an integer1: '))
	
if (num1, num2) > 0:
	product = multiply_number(num1, num2)
print(product)
	
#else:	
		